<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="Cup">
    <insert id="insertTeam" parameterType="CupTeam">
        insert into CupTeam(TEAM_NAME, TEAM_LEADER, TEAM_PHONE, TEAM_MAIL, TEAM_UNIFORM_HOME, TEAM_UNIFORM_AWAY, TEAM_EMBLEM, TEAM_PASSWORD, TEAM_CODE)
        values(#{teamName}, #{teamLeader}, #{teamPhone}, #{teamMail}, #{teamUniformHome}, #{teamUniformAway}, #{teamEmblem}, #{teamPassWord}, #{teamCode})
    </insert>
     <select id="selectTeam" resultType="CupTeam">
        select * from CUPTEAM
    </select>
     
    <insert id="insertPlayer" parameterType="CupPlayer">
        insert into CupPlayer(PLAYER_NAME, PLAYER_PHOTO, PLAYER_GOAL, PLAYER_YELLOW_CARD, PLAYER_RED_CARD)
        values(#{playerName}, #{playerPhoto}, #{playerGoal}, #{playerYellowCard}, #{playerRedCard})
    </insert>
     <select id="selectPlayer" resultType="CupPlayer">
        select * from CupPlayer
    </select>
  
    <resultMap id="CupPlayerResultMap" type="com.zero.domain.CupPlayer">  
        <id property="playerNo" column="PLAYER_NO"/>
        <result property="playerName" column="PLAYER_NAME"/>
        <result property="playerPhoto" column="PLAYER_PHOTO"/>
        <result property="playerGoal" column="PLAYER_GOAL"/>
        <result property="playerYellowCard" column="PLAYER_YELLOW_CARD"/>
        <result property="playerRedCard" column="PLAYER_RED_CARD"/>
        <collection property="cupTeam" resultMap="CupTeamResultMap"/>
    </resultMap>
    
    <resultMap id="CupTeamResultMap" type="com.zero.domain.CupTeam">
        <id property="teamNo" column="TEAM_NO"/>
        <result property="teamName" column="TEAM_NAME"/>
        <result property="teamLeader" column="TEAM_LEADER"/>
        <result property="teamPhone" column="TEAM_PHONE"/>
        <result property="teamMail" column="TEAM_MAIL"/>
        <result property="teamUniformHome" column="TEAM_UNIFORM_HOME"/>
        <result property="teamUniformAway" column="TEAM_UNIFORM_AWAY"/>
        <result property="teamEmblem" column="TEAM_EMBLEM"/>
        <result property="teamPassWord" column="TEAM_PASSWORD"/>
        <result property="teamCode" column="TEAM_CODE"/>
        <result property="teamMemberCnt" column="TEAM_MEMBER_CNT"/>
        <result property="teamRegYear" column="TEAM_REGYEAR"/>
        <result property="branchNo" column="BRANCH_NO"/>
        <result property="scoreNo" column="SCORE_NO"/>
    </resultMap>
  
	<select id="selectCupTeamAndPlayers" resultMap="CupPlayerResultMap">
	    SELECT
	         p.*, t.*
	    FROM
	        CupPlayer p
	    JOIN
	        CupTeam t
	    ON
	        p.TEAM_NO = t.TEAM_NO
	</select>

</mapper>



